### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon


# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
### chezmoi: intentionally local-only
# Coding agent configs stay off this public repo. They accumulate per-directory
# trust entries, MCP server definitions and project paths that point at work
# repos, and re-adding them keeps dragging that back in. Manage them by hand.
dot_claude/
dot_codex/
dot_config/opencode/
# Machine-local shell config (see dot_zshrc).
dot_zsh.d/99-local.sh

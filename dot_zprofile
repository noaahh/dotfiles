# Initialize Homebrew once for login shells.
if [[ -x /opt/homebrew/bin/brew ]]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

path+=("/Applications/Obsidian.app/Contents/MacOS")
typeset -U path
